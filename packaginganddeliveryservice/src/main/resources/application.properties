# Server Port (Port 8082 matching OpenFeign in ComponentProcessing)
server.port=8082

# Application Name
spring.application.name=packaginganddeliveryservice

# Actuator Health Check
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always